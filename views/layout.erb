<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Engines first run</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {font-family: sans-serif; font-size: 16px; margin: 20px; background-color: #48d; }
    div.first_run_form { border: 1px solid #ccc; width: 400px; margin: auto; padding: 0px 20px; border-radius: 6px; background-color: #fff; }
    h3 { color: #666; }
    hint { font-size: 14px; color: #666; display: none; }
    #toggle_hints { float: right; margin: 10px; padding: 8px; color: #aaa; background-color: #fff; border: 1px solid #ccc; border-radius: 18px; height: 35px; width: 35px; }
    #toggle_hints:hover { cursor: pointer; }
    legend { border-bottom: 1px solid #ccc; padding: 5px; }
    .field_group { margin: 10px; }
    input, select { margin-left: 10px; max-width: 244px; }
    input#form_submit_button { background-color: #337ab7; color: #fff; padding: 6px 12px; font-size: 16px; border: 1px solid #2e6da4; border-radius: 4px; }
    input#form_submit_button:hover { background-color: #286090; border-color: #204d74; }
    code { color: red; }
    .error h4 { color: #966; }
    .success h4 { color: #696; }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
